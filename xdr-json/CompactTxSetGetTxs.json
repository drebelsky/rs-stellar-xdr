{
  "$schema": "https://json-schema.org/draft/2019-09/schema",
  "title": "CompactTxSetGetTxs",
  "description": "CompactTxSetGetTxs is an XDR Struct defined as:\n\n```text struct CompactTxSetGetTxs { Hash txSetHash; // differentially encoded indices of transactions requested opaque indices<>; }; ```",
  "type": "object",
  "required": [
    "indices",
    "tx_set_hash"
  ],
  "properties": {
    "$schema": {
      "type": "string"
    },
    "indices": {
      "type": "string",
      "contentEncoding": "hex",
      "contentMediaType": "application/binary"
    },
    "tx_set_hash": {
      "type": "string",
      "maxLength": 64,
      "minLength": 64,
      "contentEncoding": "hex",
      "contentMediaType": "application/binary"
    }
  },
  "unevaluatedProperties": false
}